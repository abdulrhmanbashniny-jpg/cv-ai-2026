## خطة إعادة الهيكلة الشاملة

### المرحلة 1: قاعدة البيانات
- إنشاء جدول `premium_orders` لتتبع طلبات النماذج المميزة (template_id, lead_id, status, paid_at)
- إنشاء جدول `notification_settings` لإعدادات التنبيهات

### المرحلة 2: مركز قيادة الوكلاء (MODULE 1)
- حذف التبويبات المنفصلة: سجل المحادثات، إدارة الذكاء، إعدادات الوكلاء
- إنشاء `AdminAICommandCenter.tsx` موحد مع:
  - اختيار الوكيل (Twin, Advisor, CV Helper)
  - 3 تبويبات لكل وكيل: الإعدادات، الذاكرة (قاعدة المعرفة)، السجلات
  - زر "تصحيح" بجوار كل رد AI

### المرحلة 3: سير عمل النماذج المميزة (MODULE 2)
- تحديث Edge Function `template-download` ليرسل تنبيه Telegram للنماذج المميزة
- رسالة نجاح + رابط WhatsApp مُعبأ مسبقاً في تنبيه Telegram
- إضافة قسم "الطلبات" في Admin مع زر "تم الدفع"
- زر "تم الدفع" يفتح WhatsApp برسالة تسليم جاهزة (بدلاً من إيميل تلقائي مبدئياً)
- لاحقاً: إعداد نظام البريد الإلكتروني لإرسال الملفات تلقائياً

### المرحلة 4: CAIO التفاعلي (MODULE 3)
- ترقية `AdminCAIO.tsx` بإضافة واجهة دردشة تفاعلية
- CAIO يصل لبيانات: الطلبات، المحادثات، التنزيلات، العملاء

### المرحلة 5: مركز التنبيهات + إصلاحات UI (MODULE 4)
- إنشاء `AdminNotifications.tsx` لتتبع الأحداث + تفعيل/تعطيل Telegram
- تغيير "خدمات الشركات" → "بوابة التوظيف" في Navbar
- تقييد PromoBar و WelcomePopup على الصفحة الرئيسية فقط

### المرحلة 6: إصلاحات UX (MODULE 5)
- إصلاح التمرير التلقائي في صفحة الاستشارات
- إضافة `beforeunload` في صفحات الاستشارة وهدية السيرة الذاتية
